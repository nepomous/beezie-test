/**
 * Shared sizing/backdrop values for the centered "modal card" shell used by
 * `PaymentModal` and `WhatYouCanPullScreen` (and the box `PurchaseFlowModal`
 * animates between the two), so all three stay in sync instead of each
 * hardcoding its own copy of these numbers.
 */
export const MODAL_CARD_MAX_WIDTH = 760;
export const MODAL_OVERLAY_PADDING = 24;
export const MODAL_BACKDROP_COLOR = "rgba(0, 0, 0, 0.7)";
